using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpaceTrigger : MonoBehaviour
{
    /*
    public int id;
    private void Update() {
        if(Input.GetKeyDown(KeyCode.Space)){
            GameEventsManager.Instance.SpaceEnter(id);
        }
    }
    */
}
