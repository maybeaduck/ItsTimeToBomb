using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HiController : MonoBehaviour
{
    /*
    public int id;
    private void OnEnable() {
        GameEventsManager.Instance.OnSpaceEnter += SayHi;
    }
    void SayHi(int id){
        if (id == this.id)
        {
            Debug.Log("Hi"+ id);
        }
    }
    private void OnDestroy() {
        GameEventsManager.Instance.OnSpaceEnter -= SayHi;
    }
    */
}
